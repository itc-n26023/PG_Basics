# 波カッコ { } を使って「辞書（ディクショナリ）」を作る
# 「キー（名前）: 値（中身）」というペアでデータを管理 する。
fruits = {
    "Apple": "Red",      # アップル（鍵）といえば、赤（値）
    "Banana": "Yellow"   # バナナ（鍵）といえば、黄色（値）
}

# 辞書の中身をまとめて画面に表示する
print(fruits)
